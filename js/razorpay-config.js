/**
 * Razorpay Standard Checkout configuration.
 * Key ID is loaded from /api/config at runtime. Never put Key Secret in frontend code.
 */
var RAZORPAY_CONFIG = {
  apiBase: '',
  businessName: 'Hey Upasna',
  businessDescription: 'Therapy session booking'
};
